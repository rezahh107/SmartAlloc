name: E2E (optional)
on:
  workflow_dispatch:
  # optional:
  # schedule:
  #   - cron: '0 4 * * 6'
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Trigger manually when you need full end-to-end checks."
