{
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.41.2",
    "@wp-playground/cli": "^2.0.10",
    "adm-zip": "^0.5.16",
    "axe-core": "^4.10.0",
    "playwright": "^1.41.2",
    "start-server-and-test": "^2.0.3"
  },
  "scripts": {
    "e2e:install": "npx playwright install --with-deps chromium",
    "test:e2e": "playwright test --reporter=list,junit --output=build/e2e --workers=2",
    "test": "echo \"No JS tests yet\"",
    "sec:snyk": "if command -v snyk >/dev/null; then snyk test --severity-threshold=medium || true; else echo 'snyk not installed, skipping'; fi"
  }
}
